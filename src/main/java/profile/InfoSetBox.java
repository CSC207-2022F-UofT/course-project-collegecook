package profile;

/**
 *This is an interface between the view layer and the presenter layer. Related UI should implement this interface.
 */

public interface InfoSetBox {
    /**
     * Present the interface of setInfoSuccess when setting the user's information.
     */
    void setInfoSuccess();
}
